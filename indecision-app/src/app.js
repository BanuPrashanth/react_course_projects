console.log("App.js is Running!!!");

//JSX - JAVASCRIPT XML

var template = <h1>This is a JSX Template! Are the Changes Seen? Yes it's seen..</h1>
var appRoot = document.getElementById('app');

ReactDOM.render(template, appRoot);